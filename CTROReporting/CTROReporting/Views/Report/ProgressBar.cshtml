@using Microsoft.AspNet.Identity
@model CTROReporting.ViewModels.ReportProgressViewModel

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CTROReporting</title>

    @Styles.Render("~/Content/CSS")
    @Scripts.Render("~/Scripts/HomeLayout")

</head>
<div>
    <div class="progress">
        <div id="reportprogress" class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="width: @(Model.ProgressPercentage)">
            @Model.ProgressPercentage
        </div>
    </div>
</div>
<script type="text/javascript">
    @*$(function () {
        var current_progress = 0;
        var interval = setInterval(function () {
            current_progress = @Model.ProgressPercentage;
            $("#reportprogress")
                .css("width", current_progress + "%")
                .attr("aria-valuenow", current_progress)
                .text(current_progress + "%");
            if (current_progress >= 100)
                clearInterval(interval);
        }, 500);
    });*@

    @*$(function () {
        var current_progress = @CTROReporting.CTRO.CTROFunctions.processpercentage;
        var interval = setInterval(function () {
            current_progress = @CTROReporting.CTRO.CTROFunctions.processpercentage;
            $("#reportprogress")
                .css("width", current_progress + "%")
                .attr("aria-valuenow", current_progress)
                .text(current_progress + "% ");
            if (current_progress >= 100)
                clearInterval(interval);
        }, 1000);
    });*@
</script>

